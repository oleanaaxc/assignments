Question 1a:
input1 = input("Enter something: ")
input2 = input("Enter something: ")
input3 = input("Enter something: ")
input4 = input("Enter something: ")
input5 = input("Enter something: ")

my_tuple = (input1, input2, input3, input4, input5)
print(my_tuple)

Question 1b:
To assign a tuple with only one element, the best way is to begin with your assigned tuple ex: my_tuple and then in the parenthesis make sure there is a comma after said element ex: (1, )

Question 1c:
3  4  4  2  1

Question 1d:
Part C has 14 elements while "my_tuple = my_tuple + my_tuple" has 28.
code:
my_tuple = (1,2,3,4,3,2,1,2,3,5,4,3,2,1)

print(len(my_tuple))
my_tuple = my_tuple + my_tuple
print(len(my_tuple))

Question 1e:
In "x.append(1)", .append cannot be used with a tuple but instead a list. Also in "x[1] = "hello"" the element is not able to be changed into hello because tuples are immutable after created, as are there elements same with "del x[2]".

Question 2a:
Each variable is an integer (data type).


Question 2c:
(1, [2, 3], 4)

Question 3:
the memory addresses are assigned according to the memory address of the integer and re-used when it is the same integer. Ex: 200 is shown as 140705606532440 twice and they're not in top/bottom order but instead in memory address order.

Challenges:
