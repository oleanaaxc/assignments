My final Python code:

patient = {}

def insert(first_name, last_name, birth_month):
    record_number = len(patient) + 1

    patient[record_number] = {
        "First Name": first_name,
        "Last Name": last_name,
        "Birth Month": birth_month
    }


for i in range(2):
    first_name = input("Enter patient's first name: ")
    last_name = input("Enter patient's last name: ")
    birth_month = int(input("Enter birth month when vaccine was administered: "))

    insert(first_name, last_name, birth_month)

print(patient)
